<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.marek.komunikator">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:name=".ActivityManager"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".contacts.ContactsActivity"
            android:label="@string/title_activity_contacts" />
        <activity
            android:name=".userSettings.activities.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".userSettings.activities.RegisterActivity"
            android:label="@string/title_activity_register" />
        <activity android:name=".UserDataActivity" />
        <activity
            android:name=".userSettings.activities.ChangePasswordActivity"
            android:label="@string/title_activity_change_password" />
        <activity
            android:name=".userSettings.activities.RemindPasswordActivity"
            android:label="@string/title_activity_remind_password" />
        <activity android:name=".PremiumActivity" />
        <activity android:name=".contacts.FindContactActivity" />
        <activity android:name=".messages.MessageActivity"></activity>
    </application>

</manifest>